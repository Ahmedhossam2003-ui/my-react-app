all:
  children:
    sql servers:
      hosts:
        - sql_db1:
            ansible_host: sql01.xyz.com
            ansible_connection: ssh
            ansible_os: Linux
            ansible_user: root
            ansible_user_password: Lin$Pass

        - sql_db2:
            ansible_host: sql02.xyz.com
            ansible_connection: ssh
            ansible_os: Linux
            ansible_user: root
            ansible_user_password: Lin$Pass

    web servers:
      hosts:
        - web_node1:
            ansible_host: web01.xyz.com
            ansible_os: Win
            ansible_user: administrator
            ansible_user_password: Win$Pass

        - web_node2:
            ansible_host: web02.xyz.com
            ansible_os: Win
            ansible_user: administrator
            ansible_user_password: Win$Pass

        - web_node3:
            ansible_host: web03.xyz.com
            ansible_os: Win
            ansible_user: administrator
            ansible_user_password: Win$Pass
